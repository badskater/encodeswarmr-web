---
import DocsLayout from '../../layouts/DocsLayout.astro';
---
<DocsLayout title="Architecture">
  <h1>Architecture</h1>
  <p>
    EncodeSwarmr is a distributed video encoding orchestration system built around a Controller-Agent model. The Controller is a single Go binary that owns all state; Agents are lightweight worker processes that run on encoding machines and communicate with the Controller over mTLS gRPC.
  </p>

  <h2>System Overview</h2>
  <pre><code>
┌─────────────────────────────────────────────────────────┐
│                       Browser / API Client              │
│   React SPA (Vite)          REST /api/v1     WebSocket  │
└───────────────┬─────────────────┬──────────────┬────────┘
                │  HTTP :8080     │              │
┌───────────────▼─────────────────▼──────────────▼────────┐
│                     Controller (Go)                      │
│                                                          │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐  │
│  │  Job Queue  │  │  Scheduler   │  │  Template Eng. │  │
│  │  (priority  │  │  (FIFO+      │  │  (AviSynth/    │  │
│  │   FIFO)     │  │   priority)  │  │   VapourSynth) │  │
│  └─────────────┘  └──────────────┘  └────────────────┘  │
│                                                          │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐  │
│  │  PostgreSQL │  │  WebSocket   │  │  Prometheus    │  │
│  │  (state)    │  │  Event Hub   │  │  /metrics      │  │
│  └─────────────┘  └──────────────┘  └────────────────┘  │
└──────────────────────────┬──────────────────────────────┘
                           │  gRPC :9443 (mTLS)
          ┌────────────────┼────────────────┐
          │                │                │
┌─────────▼──────┐ ┌───────▼──────┐ ┌──────▼───────┐
│   Agent 01     │ │   Agent 02   │ │   Agent 03   │
│  (NVIDIA GPU)  │ │  (Intel QSV) │ │  (CPU only)  │
│                │ │              │ │              │
│  SQLite journal│ │ SQLite journl│ │ SQLite journl│
└────────────────┘ └──────────────┘ └──────────────┘
  </code></pre>

  <h2>Key Features</h2>
  <ul>
    <li><strong>Controller-Agent separation</strong> — state lives exclusively in the Controller; agents are stateless workers that pull work via gRPC streaming RPCs.</li>
    <li><strong>mTLS authentication</strong> — all gRPC traffic is encrypted and mutually authenticated using per-agent certificates issued by the Controller's built-in CA.</li>
    <li><strong>Priority FIFO scheduling</strong> — jobs carry an integer priority; within the same priority the queue is first-in, first-out. The scheduler matches jobs to capable agents based on codec requirements and GPU availability.</li>
    <li><strong>Adaptive chunked encoding</strong> — the Controller splits long sources into segments and distributes chunks across multiple agents in parallel, then concatenates the output.</li>
    <li><strong>Offline resilience</strong> — agents maintain a local SQLite journal; they can survive a Controller outage and resume reporting when connectivity is restored.</li>
    <li><strong>Template-driven script generation</strong> — AviSynth and VapourSynth filter scripts are generated from Go <code>text/template</code> templates with custom functions, allowing per-source variable injection and custom filter chains.</li>
    <li><strong>Real-time WebSocket events</strong> — all job state transitions, agent heartbeats, and log lines are broadcast over WebSocket so the React dashboard updates without polling.</li>
    <li><strong>Plugin-based codec system</strong> — encoding backends are Go interface implementations registered at startup, making it straightforward to add new codecs.</li>
    <li><strong>Prometheus metrics</strong> — the Controller and each Agent expose <code>/metrics</code> endpoints compatible with Prometheus scraping and the included Grafana dashboards.</li>
  </ul>

  <h2>Architecture Decision Records</h2>
  <p>Significant architectural choices are documented as Architecture Decision Records (ADRs). See the <a href="/docs/adr">ADR Index</a> for the full list of decisions and their current status.</p>

  <h2>Repository Layout</h2>
  <pre><code>encodeswarmr/
├── cmd/
│   ├── controller/        # Controller binary entrypoint
│   └── agent/             # Agent binary entrypoint
├── internal/
│   ├── agent/             # Agent service, runner, GPU, progress, offline store
│   ├── controller/        # API, engine, scheduler, webhooks, auth, plugins, presets
│   ├── db/                # PostgreSQL migrations and query layer (pgx)
│   └── proto/             # gRPC service definitions
├── proto/                 # Protobuf source files
├── api/                   # OpenAPI spec and SDK generation configs
├── web/                   # React 19 + Vite 8 static build
├── configs/               # Example YAML configs
├── deploy/
│   ├── ansible/           # Ansible roles and playbooks
│   ├── terraform/         # Terraform modules (AWS, Azure, GCP)
│   └── helm/              # Kubernetes Helm chart
├── deployments/           # Dockerfile, docker-compose, Patroni
├── scripts/               # Install scripts (controller + agent)
├── installer/             # Windows Inno Setup installer
├── tests/integration/     # 3-layer integration test suite
├── docs/                  # ADRs, Grafana, capacity planning, HA, cloud storage
└── Makefile</code></pre>
</DocsLayout>
